#include <stdio.h>
/**
 * main - the point of entry of the program
 * @argc: count of the argument
 * @argv: argument vector
 * Return: 0
 */
void main(int __attribute__((unused))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
